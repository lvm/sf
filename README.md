# sf
s{earch,ound} files

```
usage: sf.py [-h] [-s] [-t] [-lt LESS_THAN] [-gt GREATER_THAN] [-eq EQUAL_TO]
             [-n NAME] [-ls] [-v]

optional arguments:
  -h, --help            show this help message and exit
  -s, --search          Search `samples` by different methods. --time +
                        --less-than --time + --greater-than --time + --equal-
                        to --name Also with -v prints also the wave files
                        within.
  -t, --time            Search `samples` by different methods. --time +
                        --less-than --time + --greater-than --time + --equal-
                        to --name Also with -v prints also the wave files
                        within.
  -lt LESS_THAN, --less-than LESS_THAN
                        Search `samples` by different methods. --time +
                        --less-than --time + --greater-than --time + --equal-
                        to --name Also with -v prints also the wave files
                        within.
  -gt GREATER_THAN, --greater-than GREATER_THAN
                        Search `samples` by different methods. --time +
                        --less-than --time + --greater-than --time + --equal-
                        to --name Also with -v prints also the wave files
                        within.
  -eq EQUAL_TO, --equal-to EQUAL_TO
                        Search `samples` by different methods. --time +
                        --less-than --time + --greater-than --time + --equal-
                        to --name Also with -v prints also the wave files
                        within.
  -n NAME, --name NAME  Search `samples` by different methods. --time +
                        --less-than --time + --greater-than --time + --equal-
                        to --name Also with -v prints also the wave files
                        within.
  -ls, --list           Prints the `samples`. With -v prints also the wave
                        files within.
  -v, --verbose         Prints the `samples`. With -v prints also the wave
                        files within.
```
